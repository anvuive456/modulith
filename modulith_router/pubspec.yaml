name: modulith_router
description: "Navigation 2.0 routing for modulith: nested route modules mounted through RoutingView outlets, with guards, deep links and scoped DI."
version: 0.2.0
repository: https://github.com/anvuive456/modulith
issue_tracker: https://github.com/anvuive456/modulith/issues

environment:
  sdk: ^3.12.2
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.9.0
  # Resolved from pub.dev when published; `pubspec_overrides.yaml` points it
  # back at ../modulith for work inside this repo.
  modulith: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
